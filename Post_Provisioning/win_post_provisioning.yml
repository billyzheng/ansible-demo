---
- name: windows post provisioning  
  hosts: all
  gather_facts: no
  collections:
    - ansible.windows
  tasks:

  - name: Validate and Modify the system timezone
    ansible.builtin.import_role:
      name: timezone
