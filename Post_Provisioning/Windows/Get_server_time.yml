---
- name: Server time Identification
  hosts: localhost
  gather_facts: no
  tasks:      
    - name: Convert date to epoch
      command: TZ="Europe/Amsterdam" date -d "20 Aug 2024 20:00:00" +%s
      register: result

    - name: Debug output
      debug:
        msg: "The epoch time is {{ result.stdout }}"
